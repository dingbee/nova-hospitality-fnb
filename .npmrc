# Vercel currently invokes npm install. The dependency tree contains legacy peer ranges that npm's strict resolver rejects.
# Keep installation deterministic across Vercel and local npm environments until the dependency tree is fully lockfile-pinned.
legacy-peer-deps=true
