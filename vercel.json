{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "tanstack-start",
  "installCommand": "corepack enable && corepack prepare pnpm@10.15.0 --activate && pnpm config set registry https://registry.npmjs.org/ && pnpm install --no-frozen-lockfile --child-concurrency=1 --network-concurrency=1"
}
